#include<stdio.h>
int main()
{
    int i=0;
    for(i=100;i<=200;i++)
    {
        //判断素数
        //试除法
        //产生 2- i-1
        int j=0;
        for(j=2;j< i;j++)
        {
            if(i%j ==0)
        {
            break;
        }
        }
        if(j ==i)
        {
            printf("%d \n", i);
        }

    }

    return 0;
}
