int main()
{
//    int i = 0,a = 0,b = 2, c = 3, d = 4;
//    i = a++ && ++b && d++;//a为假，全体为假，后面不算
    int i = 0,a = 1, b = 2, c = 3, d = 4;
    i = a++ || ++b || d++;//a为真，全体为真，后面不算;
    printf("a = %d, b = %d, c = %d, d = %d ",a,b,c,d );
    return 0;
}
