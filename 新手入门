// = 赋值 == 相等   常量 变量 两者 建议常量放左边（容易找出错误）否则有时候 = 与 == 会出错 但你不知道
#include<stdio.h>

int main()
{
    int a = 4;
    if(5 == a)
    {
        printf("shuai");

    }
    else
    {
        printf("帅逼");
    }
    //if(num = 5);// = 赋值 == 相等   常量 变量 两者 建议常量放左边（容易找出错误）否则有时候 = 与 == 会出错 但你不知道
    return 0;
}
